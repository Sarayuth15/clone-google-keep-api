spring:
  datasource:
    url: jdbc:mysql://localhost:3306/google_keep_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: yuthMySQL$$168
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: always          # always run schema.sql on startup
      schema-locations: classpath:schema.sql

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none        # Hibernate does NOT touch the schema — schema.sql owns it
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  jackson:
    date-format: yyyy-MM-dd'T'HH:mm:ss
    time-zone: UTC

server:
  port: 8080

app:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expiration: 86400000
    refresh-expiration: 604800000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method

logging:
  level:
    org.hibernate.SQL: DEBUG
    com.googlekeep: DEBUG
